# ASysGuard

A small metric export service for Linux, plus an Android metric display application.

## Components

* [sysguard-exporter](sysguard-exporter) - Linux user systemd service that exports a metric via HTTP
* ASysGuard (this directory) - Main Android metric display application

## Build

Follow build instructions for each subproject
